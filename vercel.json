{
  "buildCommand": "npm.cmd run build --prefix client",
  "outputDirectory": "client/dist",
  "installCommand": "npm.cmd install --prefix client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
